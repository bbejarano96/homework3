Bryan BejaranoCOMD6336Homework 3: Part 3 2.)const addNums = () => {let num1 = document.getElementById("num1").value;let num2 = document.getElementById("num2").value;let sum = parseInt(num1) + parseInt(num2); document.getElementById("add_result").innerHTML = sum; };?const btnAdd = document.getElementById("add"); btnAdd.addEventListener("click", addNums); 3.)a.)Reference functions are stored and used later. “addEventListener()” Reference function code example: //Add?const addNums = () => { let num1 = document.getElementById("num1").value; let num2 = document.getElementById("num2").value; let sum = parseInt(num1) + parseInt(num2); document.getElementById("add_result").innerHTML = sum; }; const btnAdd = document.getElementById("add"); btnAdd.addEventListener("click", addNums);  “addNums” “addEventListener()” method. “AddNums” is stored and only used when the user clicks the button with the class add. b.)Call/invoke functions are used immediately when the website loads.let alertUser = () => {?alert("You are now viewing Homework: 3"); } alertUser(); 4.) Difference between function declaration and function expression is the function name. Named functions are hoisted like var. They load before any code is executed. Named function code example: function enterName(name) {    return ‘My name is ${name}!’}enterName(‘Bob’)5.)
Arrow functions are a compact alternate of a functionconst addNums = () => {let num1 = document.getElementById("num1").value;let num2 = document.getElementById("num2").value;let sum = parseInt(num1) + parseInt(num2); document.getElementById("add_result").innerHTML = sum; };?Anonymous functions, assigned to a variable, also known as a function expressionconst addNums = () => { let num1 = document.getElementById("num1").value;let num2 = document.getElementById("num2").value;let sum = parseInt(num1) + parseInt(num2); document.getElementById("add_result").innerHTML = sum; }; Named function, function is hoisted into the browser function namedExample() { alert("Named function");}?namedExample();6.)https://github.com/bbejarano96/toggle_circle7.) window.location.reload() The “reload()” method reloads the document, clicking the reload button on your browser. 8.)- ‘pwd’ to check the directory. - ‘git init’ initializes a local git repository to your computer. 	- type ‘git status’ on the terminal and enter. - to ‘commit’ changes type “git add .”  hit enter if you want to ‘commit’ all your files and folders in the directory - ‘commit’ individual files, ‘git add index.html’ - ‘git commit –m ‘comment’   ‘git commit’- ‘git remote add’ the remote hosting service,?that host your repository remotely. - ‘git remote add origin remoteurl’ to push local commits for the onto remote.- click your profile, click Repositories, click the button new and will bring you to create an New Repository. 		- filling repository name. Click the “create repository” button. - ‘git push –u’ using this you can push your local repository into remote origin.- master branch, default branch when you initialize by you. - ‘cd’ can change the directory. ‘cd name_of_folder’ in the terminal. 9.)The local repository is the. git/ subdirectory inside the working directory.10.) The master branch, signals an initialized Git.